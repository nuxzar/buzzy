# Copy to Vercel → Project → Settings → Environment Variables
# Never commit real secrets. Never use VITE_ prefix for these.

TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=
TENCENT_TMS_BIZ_TYPE=buzzy_nickname
TENCENT_TMS_REGION=ap-guangzhou
